# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- - Hermes on Mac mini: container issue was resolved by recreating the Docker container with `gateway run` and restoring Telegram env vars. The durable fix is to keep `TELEGRAM_BOT_TOKEN` and `TELEGRAM_ALLOWED_USERS` in `~/.hermes/.env` so a container recreate does not lose them. - OpenClaw/Ollama provider investigation findings: - Global config and main-agent allowlists were partially misaligned. Explicit allowed model entries were needed for `ollama-gigabyte/*` and `ollama-cloud/*` models. - Main agent runtime metadata file `~/.openclaw/agents/main/agent/models.json` contained malformed/duplicate Ollama provider entries (`ollama`, `ollama_cloud`, etc.). Cleanup was performed to keep the [confidence=0.60 evidence=memory/2026-05-01.md:7-13]
- # 2026-05-03 - OpenClaw cron/local-Ollama debugging status: original isolated cron failure `No API provider registered for api: ollama` was moved forward by routing a failing cron job through `heartbeat-llama`; provider-registration issue appears resolved for that path, but the job later timed out instead of completing. - Confirmed local Ollama inventory: `llama3.2:3b` is installed locally, `gemma4:e2b` is not installed locally. - User preference confirmed: all three lanes must work, with small/simple cron jobs using local `llama3.2:3b`; remove `gemma4:e2b` from the local lane because user is not using it. - Important correction: unsupported JSON keys were mistakenly added to `~/.openclaw/o [confidence=0.58 evidence=memory/2026-05-03.md:1-9]
- - OpenClaw runtime still falls back away from `ollama-gigabyte` and `ollama-cloud` because logs show `No API key found for provider "ollama-gigabyte"` and `No API key found for provider "ollama-cloud"` in the main agent auth path. - Current working conclusion: remaining blocker is provider auth mapping/auth-profile handling for Ollama-backed providers in OpenClaw, not host reachability or model availability. - OpenClaw multi-provider Ollama investigation on 2026-05-01: correct environment split is (1) native VPS-local `ollama` only for `llama3.2:3b` and `nomic-embed-text:latest`, (2) separate custom provider for Ollama-cloud carrying `kimi-k2.5:cloud`, and (3) separate custom provider fo [confidence=0.57 evidence=memory/2026-05-01.md:12-16]
