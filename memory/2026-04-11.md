
- Follow-up behavior: when Duane says `report only when done` or `don't stop until the command finishes`, keep working/polling until the result is verified, then report once.
- Issue resolved: stale OpenAI/Codex auth profile `openai-codex:default` was removed, auth order now prefers `openai-codex:duane.leitch@gmail.com`, and a real `openclaw infer model run --prompt "hello" --model openai-codex/gpt-5.4-mini` succeeded.

- Explicit instruction update: Duane wants completion requests treated as hard directives. Do not stop at intent or promise follow-up. Keep working/polling until verified done or hard failure, then report once.
- 2026-04-11, Duane explicitly asked for a stricter follow-through pattern: when asked to do something, I should do it, fix it, and report back only with verified results or a concrete blocker.
- 2026-04-11, Duane asked for operational changes to prevent repeated Discord/main-channel failures, including durable session cleanup, stable auth ordering, and safer fallback handling.
- 2026-04-11, Duane requested that any config JSON changes be backed up before editing.
