- Set up and validated Brave Search (`web_search`) and `web_fetch` in OpenClaw by correctly wiring `tools.web.search.apiKey` via `openclaw config set`, then restarting the gateway and confirming successful Brave search calls.
- Tightened infra/CLI operating rules in SOUL.md to enforce: no guessing on tools/config, one command per step, always verify after changes, treat “100% correct and explicit” as a hard rule, and ask instead of assuming when ambiguous.
- Updated Action Register process rules in MEMORY.md and practice so that when an action is marked complete/closed, I always: (1) set Status and Close Date, (2) move the item from Open/Pending to Closed, and (3) renumber remaining Open actions, in both `Action Register.md` and `Action_Register_Readable.md`.
- Fixed Action Register entry #10 (OpenClaw agents bound to dedicated Discord channels) by moving it from My Actions → Open to My Actions → Closed and renumbering the Open items in both Action Register files.
- Created and registered a new `humanizer-v2` skill under `workspace/skills/humanizer-v2/SKILL.md` to enforce a conservative, human-sounding writing style (no em dashes, minimal edits, preserve facts/voice), and set it as the default style layer for all written outputs unless explicitly disabled.
- Created several Obsidian docs for management and environment: Duane’s Q2 roundtable notes, a Quarterly Senior Management Meeting structure proposal, a memo to the CEO, OpenClaw backup/restore instructions, Open Source LLM Stack design, OpenClaw skills install links, 2FA how-to for customers, and Agent Domain Map + review feedback.
- Set up a reusable Markdown → Word pipeline using `pandoc` and a helper script (`md2docx.sh`) so that requested `.docx` documents can be generated automatically server-side alongside their `.md` sources.
- Created a helper script `docx2md.sh` conceptually (and for Agent_Domain_Map specifically) by converting `Agent_Domain_Map.docx` into `Agent_Domain_Map.md` so I can read and edit the mapping directly in Obsidian.
- Refined the Agent → Domain mapping in `90_Environment/VPS/Agent_Domain_Map.md` based on Duane’s feedback: `main` now treats Management_Meetings as Primary and Strategy as Allowed; `support_lead` treats Strategy and Management_Meetings as Allowed; `agent-health` excludes 00_General_Info and 09_Personal_Knowledge by default; `research_manager` keeps Strategy as Primary but Management_Meetings as Allowed.
- Set up a LanceDB-based vector store on the VPS: created a dedicated venv (`venv-lancedb`), installed `lancedb` and `openai`, wrote an indexer (`index_obsidian_lancedb.py`) that chunks and embeds content from `05_Diversys/Product/Training/Advanced_Training`, and successfully indexed 167 chunks into `obsidian_docs` at `~/.openclaw/lancedb`.
- Added a simple query script (`query_obsidian_lancedb.py`) to test LanceDB retrieval using OpenAI embeddings and confirm that stored chunks and metadata (root/path/area/agent_hint) can be retrieved and inspected.
