- 2026-05-01: User wants to keep using OpenAI subscription and Google Gemini in OpenClaw rather than switching to Ollama-only.
- 2026-05-01: Diagnosed that missing API key issues were more likely caused by OpenAI Codex and Google Gemini model references in agent config than by Perplexity; Perplexity was not the active provider issue.
- 2026-05-01: OpenClaw config at /home/duane/.openclaw/openclaw.json showed active model references to openai-codex/gpt-5.4, openai-codex/gpt-5.4-mini, google/gemini-2.5-flash, and google/gemini-3.1-pro-preview across default and agent fallback chains.
- 2026-05-01: During live troubleshooting, assistant stated that OpenAI and Google provider blocks had been added and that OpenAI should resolve from OPENAI_API_KEY while Gemini would require GEMINI_API_KEY; this should be verified in future work before relying on it.
## Durable notes
- Duane explicitly does not want routine Obsidian scan notifications. Only notify if a scan fails and there is something he needs to do.
- Hermes on Mac mini: container issue was resolved by recreating the Docker container with `gateway run` and restoring Telegram env vars. The durable fix is to keep `TELEGRAM_BOT_TOKEN` and `TELEGRAM_ALLOWED_USERS` in `~/.hermes/.env` so a container recreate does not lose them.
- OpenClaw/Ollama provider investigation findings:
  - Global config and main-agent allowlists were partially misaligned. Explicit allowed model entries were needed for `ollama-gigabyte/*` and `ollama-cloud/*` models.
  - Main agent runtime metadata file `~/.openclaw/agents/main/agent/models.json` contained malformed/duplicate Ollama provider entries (`ollama`, `ollama_cloud`, etc.). Cleanup was performed to keep the explicit providers such as `ollama-local`, `ollama-cloud`, `ollama-gigabyte`, and `ollama-atom`.
  - Direct raw calls to Gigabyte Ollama succeeded, confirming the host/model path works.
  - OpenClaw runtime still falls back away from `ollama-gigabyte` and `ollama-cloud` because logs show `No API key found for provider "ollama-gigabyte"` and `No API key found for provider "ollama-cloud"` in the main agent auth path.
  - Current working conclusion: remaining blocker is provider auth mapping/auth-profile handling for Ollama-backed providers in OpenClaw, not host reachability or model availability.
- OpenClaw multi-provider Ollama investigation on 2026-05-01: correct environment split is (1) native VPS-local `ollama` only for `llama3.2:3b` and `nomic-embed-text:latest`, (2) separate custom provider for Ollama-cloud carrying `kimi-k2.5:cloud`, and (3) separate custom provider for Gigabyte-hosted larger local models.
- New preferred restore point for this workstream: `/home/duane/.openclaw/restore-points/20260501-202319`. One-line rollback command previously provided restores `openclaw.json`, `models.json`, and `auth-profiles.json`, then restarts gateway.
- Important diagnosis: after correcting provider split conceptually, explicit model tests for `ollama/llama3.2:3b`, custom Ollama-cloud, and custom Gigabyte all still resolved at runtime to `openai-codex/gpt-5.4`, indicating gateway fallback/override rather than provider-structure-only failure.
- Key blocker identified: custom providers appeared with `models.json` value `marker(custom-local)` and effective auth treated as missing, so OpenClaw did not consider them eligible runtime providers and silently fell back to default OpenAI. Next troubleshooting step should focus on the exact custom-provider auth pattern OpenClaw accepts for no-auth local endpoints and API-key-backed custom endpoints.
