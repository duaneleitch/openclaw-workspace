# 2026-04-20 - Monday

## Golf Training
- **Deload Week (Week 6)** - Volume reduced to 60-70% as scheduled
- Focus on form and recovery this week

## Reminders
- Golf prep program: deload phase active
# 2026-04-20 - Monday

## Golf Training
- **Deload Week (Week 6)** - Volume reduced to 60-70% as scheduled
- Focus on form and recovery this week

## Reminders
- Golf prep program: deload phase active

## OpenClaw and Hermes troubleshooting
- OpenClaw provider routing was normalized to explicit namespaces to avoid the historical `ollama/...` collision: `ollama-cloud` -> `http://127.0.0.1:11434`, `ollama-local` -> `http://127.0.0.1:11434`, and `ollama-gigabyte` -> `http://100.109.197.98:11434`.
- Verified OpenClaw runtime after the routing fix with successful isolated tests for `ollama-gigabyte/gemma4:26b` (`LOCAL_GIGABYTE_GEMMA_OK`) and `ollama-cloud/kimi-k2.5:cloud` (`CLOUD_OK`). Final known-good OpenClaw backup is `/home/duane/.openclaw/openclaw.json.baseline-final-20260420T211122Z`.
- Hermes on the Mac mini uses host bind mount `/Users/duaneleitch/.hermes` -> container `/opt/data`. Full host backup was created before reinstall work.
- Hermes persisted old Codex state in `/opt/data/auth.json`; backup `/opt/data/auth.json.bak` was created and active file was moved aside to `auth.json.disabled` during troubleshooting.
- Reinstall finding: running `nousresearch/hermes-agent` without an explicit command launches interactive CLI mode and exits with `Warning: Input is not a terminal (fd=0). Goodbye!`. To stay up in Docker it must be started in gateway mode, e.g. container command `gateway`.
- Important model finding from Hermes logs: `hermes3:latest` is not suitable for Hermes Agent tool workflows. Switched target model to `qwen3:30b-a3b` on the Gigabyte host.
- Root cause of continued 401 after reinstall was Hermes not recognizing `provider: ollama`. Logs showed `WARNING agent.auxiliary_client: resolve_provider_client: unknown provider 'ollama'`, after which it fell through to old Codex behavior and returned `HTTP 401: No cookie auth credentials found`.
- Working Hermes config pattern for local Ollama-style endpoint appears to be `provider: custom` with an OpenAI-compatible base URL ending in `/v1`. Current top of `/Users/duaneleitch/.hermes/config.yaml` was updated to:
  - `default: qwen3:30b-a3b`
  - `provider: custom`
  - `base_url: http://100.109.197.98:11434/v1`
- Last confirmed state before compaction: Hermes container had been restarted after changing to `provider: custom`; next required verification step is an end-to-end Telegram test with prompt `Reply with exactly: HERMES_LOCAL_OK` to confirm the Codex 401 is gone.
# 2026-04-20 - Monday

## Golf Training
- **Deload Week (Week 6)** - Volume reduced to 60-70% as scheduled
- Focus on form and recovery this week

## Reminders
- Golf prep program: deload phase active
# 2026-04-20 - Monday

## Golf Training
- **Deload Week (Week 6)** - Volume reduced to 60-70% as scheduled
- Focus on form and recovery this week

## Reminders
- Golf prep program: deload phase active

## OpenClaw and Hermes troubleshooting
- OpenClaw provider routing was normalized to explicit namespaces to avoid the historical `ollama/...` collision: `ollama-cloud` -> `http://127.0.0.1:11434`, `ollama-local` -> `http://127.0.0.1:11434`, and `ollama-gigabyte` -> `http://100.109.197.98:11434`.
- Verified OpenClaw runtime after the routing fix with successful isolated tests for `ollama-gigabyte/gemma4:26b` (`LOCAL_GIGABYTE_GEMMA_OK`) and `ollama-cloud/kimi-k2.5:cloud` (`CLOUD_OK`). Final known-good OpenClaw backup is `/home/duane/.openclaw/openclaw.json.baseline-final-20260420T211122Z`.
- Hermes on the Mac mini uses host bind mount `/Users/duaneleitch/.hermes` -> container `/opt/data`. Full host backup was created before reinstall work.
- Hermes persisted old Codex state in `/opt/data/auth.json`; backup `/opt/data/auth.json.bak` was created and active file was moved aside to `auth.json.disabled` during troubleshooting.
- Reinstall finding: running `nousresearch/hermes-agent` without an explicit command launches interactive CLI mode and exits with `Warning: Input is not a terminal (fd=0). Goodbye!`. To stay up in Docker it must be started in gateway mode, e.g. container command `gateway`.
- Important model finding from Hermes logs: `hermes3:latest` is not suitable for Hermes Agent tool workflows. Switched target model to `qwen3:30b-a3b` on the Gigabyte host.
- Root cause of continued 401 after reinstall was Hermes not recognizing `provider: ollama`. Logs showed `WARNING agent.auxiliary_client: resolve_provider_client: unknown provider 'ollama'`, after which it fell through to old Codex behavior and returned `HTTP 401: No cookie auth credentials found`.
- Working Hermes config pattern for local Ollama-style endpoint appears to be `provider: custom` with an OpenAI-compatible base URL ending in `/v1`. Current top of `/Users/duaneleitch/.hermes/config.yaml` was updated to:
  - `default: qwen3:30b-a3b`
  - `provider: custom`
  - `base_url: http://100.109.197.98:11434/v1`
- Last confirmed state before compaction: Hermes container had been restarted after changing to `provider: custom`; next required verification step is an end-to-end Telegram test with prompt `Reply with exactly: HERMES_LOCAL_OK` to confirm the Codex 401 is gone.
- Verified after session reset that Hermes replied exactly `HERMES_LOCAL_OK` and then self-reported `Model: qwen3:30b-a3b` and `Provider: custom`, confirming the local Gigabyte-backed route is working.
- Root cause of the final Hermes misrouting was persisted per-session state. Deleting stuck session files for session `20260420_215454_e48b8dee` and restarting Hermes forced a fresh session to inherit the corrected config.
- Hermes container currently lacks Docker capability internally: `docker` binary not found and `/var/run/docker.sock` not mounted. This does not affect normal chat/model performance, only Docker-dependent tool paths.
- Duane decided to enable Docker access in Hermes next, after understanding the security tradeoff and that it is optional for normal chat/model use.
