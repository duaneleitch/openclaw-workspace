# 2026-03-26

- Identified and fixed a critical security issue: `.gitignore` had been misconfigured to ignore everything except `openclaw.json`, causing daily commits of live tokens and API keys to GitHub.
- Removed `openclaw.json` from git tracking, corrected `.gitignore` to explicitly exclude `openclaw.json` and other JSON configs, and committed the fix.
- Used `git filter-branch` and a forced push to rewrite repository history and remove `openclaw.json` from all historical commits.
- Rotated all externally meaningful exposed credentials:
  - Discord bot tokens for Alfred (VPS) and Elliot (Mac / NemoClaw).
  - Telegram bot tokens for Alfred and Elliot.
  - Brave Search API key used by Alfred.
- Confirmed other exposed values (gateway auth, internal hook tokens, Ollama local API details, Gmail push token) are internal to the VPS environment and do not need immediate rotation.
- Reconfirmed Alfred’s gateway is healthy: Telegram and Discord channels are enabled and working with new tokens, and Brave web search is functional with the new API key.
- Tightened Telegram access controls:
  - Elliot (NemoClaw) continues to use `allowFrom` + `groupAllowFrom` allowlists and `groupPolicy="allowlist"`.
  - Alfred (VPS) now also has `channels.telegram.allowFrom = ["7764426016"]` (Duane’s Telegram user ID) plus `dmPolicy = "pairing"`, so only Duane can talk to Alfred via Telegram.
- Confirmed that NemoClaw Discord issues on Elliot are due to a proxy/WebSocket limitation, not mis-onboarding; Discord is intentionally disabled there in favor of Telegram.
- Clarified operational pattern: NemoClaw setups use `nemoclaw onboard` on the Mac host; `openclaw onboard` is only for standalone OpenClaw installs like Alfred on the VPS.

## Late session — Infrastructure cleanup and Elliot duplicate message investigation

- **VPS old backups removed:** `sudo rm -rf /backup` executed and verified. VPS disk freed from 65% used to 25% used (39 GB recovered, 73 GB free on 96 GB volume).
- **VPS disk is now healthy:** No local backup directory remains; all backups go to Mac mini external SSD at `/Volumes/AI-Storage/Backups/VPS/` via rsync over SSH.
- **Elliot Telegram duplicate message investigation:**
  - Symptom: Elliot's bot sends identical messages twice to the same Telegram channel at the same timestamp.
  - Ruled out: multiple gateway processes (confirmed only one `openclaw-gateway` PID 66688 was running in the NemoClaw container).
  - Confirmed: gateway runs inside Docker container `openshell-cluster-nemoclaw`, not on Mac mini host directly.
  - The `openclaw gateway stop/start` commands do not work via `docker exec` because `openclaw` binary is not in PATH in the NemoClaw container environment — NemoClaw manages the gateway internally.
  - The kill-by-PID approach (kill 66688) stopped the gateway successfully, but restart must be done through NemoClaw's own process management, not via direct `openclaw` CLI.
  - `nemoclaw elliot status` output shows NIM policy is active with Telegram allowed (GET/POST to `api.telegram.org/bot*/**`), and NIM status shows `not running` after the kill.
  - Root cause hypothesis: Two separate Telegram send paths inside NemoClaw's config are both wired to the same bot token and chat ID — one for direct assistant replies and one for a mirror/channel integration.
  - **Next step (deferred to next session):** Open Elliot's NemoClaw config, find the Telegram-related stanzas, identify the duplicate send path, and disable or retarget one of them.
  - **Note for next session:** Do not try to restart Elliot's gateway via `docker exec openclaw gateway start` — this does not work. The correct restart path is through `nemoclaw` CLI on the Mac mini host.
- **Lesson learned:** When working with Duane step-by-step on CLI/infra, give one command at a time and wait for results before providing the next step. Do not provide multiple steps ahead or jump to conclusions.

- **Current canonical Diversys Obsidian root:** `/mnt/obsidian/00_Alfred/10_Diversys`. Use the `00_Alfred/10_Diversys` tree for all current Diversys product, API, training, client, and support documentation.
- **Current path remap confirmed:** `/mnt/obsidian/00_Alfred/10_Diversys/Product/API_Knowledge/2025_API_Basics_Guidebook_Swagger_Extract.md` exists and `Product_KB.md` now marks the Swagger guidebook extract as completed and verified.
