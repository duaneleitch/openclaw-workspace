# Memory — 2026-03-24

## Mac Mini Elliot — NemoClaw Setup Progress (morning session)

- AI-Storage APFS encrypted volume: password confirmed saved to Keychain via Disk Utility unmount/remount
- Docker Desktop configured: "Start on login" checked, "Open Dashboard on start" unchecked
- NemoClaw gateway container: `openshell-cluster-nemoclaw` — set `--restart=always` to prevent gateway dying on Docker restart
- Gateway recovery sequence (for future reference):
  1. `docker stop openshell-cluster-nemoclaw`
  2. `openshell gateway start` (type y if asked to recreate)
  3. `nemoclaw onboard` (type y if sandbox elliot already exists)
  4. `docker update --restart=always openshell-cluster-nemoclaw`
  5. `nemoclaw elliot connect`
- Install journal updated at `/mnt/obsidian/00_General_Info/How_To/Mac_Mini_NemoClaw_Install_Journal.md` with Phase 9 (gateway recovery + Docker settings)
- OpenClaw version inside sandbox: `OpenClaw 2026.3.11 (29dc654)` — update to `2026.3.23-2` is available
- openclaw status shows: Nemotron 3 Super 120B as default model, no channels configured, no sessions yet
- Security audit: 4 criticals — 2 are permission issues (cannot fix from inside sandbox, not a real risk in single-tenant NemoClaw), 2 are dangerous Control UI auth flags (being addressed)
- Currently in progress: disabling `gateway.controlUi.dangerouslyDisableDeviceAuth` and `gateway.controlUi.allowInsecureAuth`

## Evening session additions (2026-03-25 ~01:00 UTC)

### Phase 12 — Network blocker confirmed
- All outbound HTTP/REST from NemoClaw sandbox is blocked — both Discord and Telegram fail
- Likely ARM64 (Apple Silicon) CNI issue in NemoClaw alpha
- Journal updated with Phase 12 block documentation
- Discord bot created: server `1485613117802152116`, channel `1485613118305599490`
- Telegram bot created via BotFather (token stored in OpenClaw config)

### Additional config applied
- `agents.defaults.sandbox.mode` set to `off` (Docker not available inside k3s)
- `agents.defaults.memorySearch.enabled` set to `false` (no embedding provider configured yet)
- `openclaw doctor --fix` run: session store and OAuth dirs created

### Policies on sandbox elliot (final state)
- discord, telegram, npm, pypi, huggingface, docker

### Tools installed on Mac host
- `claude` CLI: `npm install -g @anthropic-ai/claude-code`
- `gemini` CLI: `npm install -g @google/gemini-cli` (v0.34.0)

## Next steps (Mac Mini Elliot)
- Finish clearing security flags
- Update OpenClaw to 2026.3.23-2
- Configure providers: OpenAI, Anthropic, Gemini, NVIDIA NIM
- Set up Discord (new server + new bot token for Elliot)
- Set up Telegram (new bot)
- Copy Obsidian vault to /Volumes/AI-Storage/Obsidian
- Configure Obsidian path in OpenClaw
